# v2 Round1 评审合并修复清单（按章；执行者不得改规格外内容）

通用决定：
- U1 **8B 单 token 双口径**：1.3 首建"8B 档案"处声明"32 层为教学假设（128 KiB/token）；真实 qwen3:8b 为 36 层/144 KiB/token，见 2.1"。1.3/1.4/3.2/3.3 中所有用 128 KiB 的算式旁加"（教学口径）"。
- U2 **27B Q4_K_M 多口径**：各章出现"约 25G/17G"处补"（视频口播口径；标准口径 16.5G 见 2.4）"。新建附录 C《全书数字口径总表》收纳四口径与 prefill/decode 实测行。
- U3 **千问 27B 标注补缺**：1.2/3.1/3.3/3.4/4.1/4.2 每章首次出现"千问 27B/千问 3.6 27B"处补"（口播转写型号，以模型卡为准）"。
- U4 **P106=24G 不存在**：1.2/4.3 改为"另一张 24G 帕斯卡卡（口播转写作 P106，型号存疑；P106 官方规格为 6G）"，并复核 48G 前提表述。
- U5 **MI50/MI250 统一**：全文统一 MI50，首现注明"卡型号口播转写存疑"。
- U6 **"卷二/卷三…"全部改为"第 2.3 章/第 X 部分"式引用**（grep 全书修改）。
- U7 **附录 B/C 创建**（主 agent 完成），各章"见附录 B/C"引用即成立，不改。

## 1.1
1. 高：能力清单第三条"数出一个句子的 token 数并解释按 token 计费"→ 改为"从文件名与标注读出模型的体积档位"（token 计数在 1.2/1.4）
2. 低：能力清单引用"（算例与理解检查 1）"→ 改"（正文与完整算例）"
3. 低："第三章会给它正式的名字（TTFT）"→"下一章会给它起名，3.1 给出严格口径"
4. 高：curl 首现加一句"（命令行里发 HTTP 请求的工具，相当于在终端里提交网页表单）"
5. 中：FP16/Q4/IQ3 档位词首现统一"名字＋一句话"并标注"2.3 逐步拆解"
6. 低：小练习（3 分钟）标注"需先完成动手实验的下载步骤；无环境可暂跳"
7. 低："机制在第四章"→"机制在 2.1（带宽账）与 4.3（offload）"

## 1.2
8. 高：极端样本段（P40/P106/IQ1_S/MTP/投机解码/n-gram 连发）名词砍到两个：正文只留"读题 314 tok/s、吐字 28.9 tok/s"与"权重 20 多 G"；其余名词移括注并标注"名字混个脸熟即可，后文逐一讲解"
9. 中：MTP/投机解码/INT8 首现各加一句括注（MTP=多 token 预测，一种提速技巧，3.5 讲；INT8=8 位整数量化档）
10. 中："疯狗模式"加注"（某社区调优项目的高性能档位名）"
11. 中："稠密模型"首现加"（全部参数每步都参与计算，与 MoE 相对，1.5 详解）"；1.3/1.4 的"稠密"同款括注各一次
12. 低：统一"首包"→"首字"
13. 低：IQ1_S 72G 首现加半句"（其中约一半体积是未深压的外挂大表，4.3 拆账）"
14. 高：完整算例"单张 2080Ti 装 27B Q4_K_M/INT8"→ 注明装载方式："卡为 22G 魔改版（原版 11G）"；INT8 一例改双卡口径或注明"权重下沉内存、仅示意首字差距的量级"
15. 中：U3（千问 27B 标注）
16. 低：吞吐指标名在回看框前先在正文起名（"每秒吐出的字数，行话叫吞吐"）
17. 中：U4 本章执行

## 1.3
18. 高：8B 档案首建处加教学假设声明（U1）
19. 高：GQA 定义补一句（"多个注意力头共享一组 KV 头——头数变少、KV 变小，2.1 的公式里会用到"），2.1 回指即成立
20. 低："naive 算 120G"→"朴素算法（naive）算 120G"
21. 中：实验"按第一章的分词口径估算"→"按'一个汉字≈一个 token'粗估（正式方法见 1.4）"
22. 低：demo attention/kv-cache 引导句确认仍在

## 1.4
23. 低："FPS 游戏"→"第一人称射击（FPS）游戏"
24. 中：小练习依赖 1.1 响应 → 改为"用动手实验里给出的示例响应数据"或内联一段示例
25. 中：80~100 tok/s 处加"（含 MTP 加速；裸 decode 约 50，见 3.1/3.5）"
26. 低：兑现"上下文需求"栏：1.5 选型理由栏目说明里补"上下文需求（依据 1.4 失忆复盘）"

## 1.5
27. 中："总参与激活参"→"总参数与激活参数"（该章及回查 1.2 处）
28. 低：Kimi K3"40 多个"时点注改为"推出一个多月时点"；千问 3.8"1000+/20 多天"单列
29. 中：新增"交付物地图"小表（T1~T8 与出现章号对照，说明编号沿项目里程碑而非阅读顺序）——回应编号乱序（A#20）

## 2.1
30. 中："在第五章展开"→"在 4.1/4.2 展开"
31. 低：GQA 回指改"（GQA，1.3 点名、本节展开）"或保留（19 完成后成立）——检查即可
32. 低：嵌入层首现加"（存放词表向量的部分）"；CUDA 首现加"（英伟达 GPU 的计算生态）"；Transformer 首现加"（本章出现的 Transformer 即前文'模型按层堆叠'的全称）"
33. 低："PP 单卡 580"→"prefill 速度（跑分表缩写 PP，口径见 3.1）单卡 580 tok/s"
34. 低：专家#8 "五六成"→"约四到六成，视卡型"
35. 低：Qwen3-8B config 36 层数值确认已标"以官方 config.json 为准"（保留）

## 2.2
36. 低：BF16 首现加"（一种 16 位格式，2.3 拆解）"；"前向"首现加"（forward，输入完整过一遍全部层的一次计算）"
37. 低：量化误差数字加口径"（社区困惑度增幅量级，非权重相对误差）"

## 2.3
38. 中：偏置/隐藏位两句拆段；2¹⁵÷2⁷ 跳步补"6 万量级落在指数 15 这一段"
39. 中：170HX 支线压成边栏（用引用块），正文末一句回扣"这是口径教训"
40. 中："张量"首现加"（多维数组）"；FFN/下投影首现加"（FFN=前馈层，每层里做'再加工'的半边；下投影=它的一半权重）"
41. 低：专家#13 Marlin 处加"（内核实现随架构与版本不同，以框架文档为准）"

## 2.4
42. 低：专家#5 升格清单改"注意力 V 投影、FFN 下投影与输出头（词表嵌入视模型而定）"
43. 低：输出头/GPTQ 各加半句定位

## 2.5
44. 低：TPOT 首现写全"Time Per Output Token"
45. 低：缓冲 4GiB 处注"保守取值；2.1 扣除法约 8GiB"
46. 低：量化误差口径同 37

## 3.1
47. 中：脚本注"Ollama 用户端口改 11434、模型名填 ollama list 显示的名字"
48. 低：U3

## 3.2
49. 中：OS 页表类比前补一句背景或加"换个比方：图书馆座位（物理页）与借书条（页表）"
50. 低："墙钟时间"→"真实流逝时间（俗称墙钟时间）"
51. 低：128 KiB 处加"（教学口径，U1）"

## 3.3
52. 中："TP worker"→"张量并行（TP）worker（多卡协作进程，4.1 展开）"
53. 低："eager 模式"加"（不建图、逐个发射的默认模式）"
54. 中：实验前加迁移说明："从 Ollama 路线来的读者：`ollama` 换 `llama-server` 或装 vLLM（各官方文档），本章 A/B 亦可用 llama.cpp 的对应开关近似；无 N 卡则标记选做、用书中数据纸面完成"

## 3.4
55. 低：U3；"128T/256T"的 T 注"（TOPS，每秒万亿次运算）"
56. 低："五六成"若出现同 34

## 3.5
57. 中：dFlash/DSpark 加框注"名称来自视频转写，厂商归属存疑；检索以正文描述的特征为准"

## 4.1
58. 中：U5；专家#8 口径（若有"五六成"）
59. 低：小练习 40 倍补条件"（prefill 按 1700 tok/s 折算为每秒流量）"

## 4.2
60. 高：NVLink"约为 PCIe 3.0 x16 的 6 倍"→"约为 PCIe 3.0 x16 双向带宽（31.5GB/s）的 3 倍余（口径：同为双向）"
61. 低：U3/U5

## 4.3
62. 高：U4（P106→口播存疑）
63. 中：补章末产出："安放决策记录（成分表＋冷热分级结论）"
64. 中：两套模型规格（72G/176B vs 约 500G/表 200G）首现处声明"这是更新一代的更大模型，非本案例模型"
65. 低："类稠密模型"加"（激活参数接近总参数的 MoE）"；"颠簸状态"加"（数据在内存与磁盘来回搬运、速度暴跌）"

## 4.4
66. 低："反向代理"加"（如 Nginx 这类转发程序）"
67. 低："视频里"已改，复查即可

## 5.1
68. 低：T10 ECC 段从"HBM 带来的现象"移到"专业卡/数据中心卡的规格表外变量"，并注明"ECC 为数据中心卡通用特性，非 HBM 特有；T10 是 GDDR6 专业卡"
69. 低：SM60/SM61 加"（SM 编号即后文 compute_cap 代际号）"；ROCm 加"（AMD 对应 CUDA 的计算生态）"
70. 低："百元卡里最高的一档"→"神卡区（千元内）里带宽最高的一档"

## 5.2
71. 高：KV 账"约 2G"→"约 1.2 GiB（64 KiB/token × 8192 × 2 路，按 2.1 的 27B 口径）"
72. 低：T4 加"（数据中心卡，与 T10 同族）"；"已出 AMD 官方支持列表"→"已移出 AMD 官方支持列表"
73. 中：25G/17G 处加 U2 口径回指

## 5.3
74. 低：η 中文为主："利用率 η（utilization）"首次后，正文用"利用率"

## 5.4
75. 中：新增"全书数字口径总表"小节（附录 C 的精简版内嵌）：27B Q4_K_M 四口径、8B KV 单 token 两口径、prefill 实测行（580/800/1700/2500/2600 ×条件）、裸 decode 上限（37/60）
76. 低："三倍超载"补算式"（5G＋36G＝41G，为 24G 卡的 1.7 倍、8G 显存的 5 倍）"；"对半排查"加"（每次排除一半可能，即二分法）"
