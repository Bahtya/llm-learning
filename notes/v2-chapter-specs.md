# v2 逐章规格（22 章；1.1 已完成，作为文风基准：content-v2/part1/1-1.md）

通用：文件路径 content-v2/partN/M-MM.md；汉字 3500~6000；源章指 v1 的 content/vX/NN.md（事实来源+可复用段落素材，但结构必须按本规格重排）；完整算例/实验/产出不得与规格偏离。

## 1.2 一次请求的旅程（part1/1-2.md）
核心问题：我发一句话之后，机器里发生了什么？
上章已掌握：部署与调用、token 计数、显存粗判。下一章依赖：逐字生成与 KV 直觉、prefill/decode 现象。
源章：v1/02、v1/07、v3/08。笔记：BV1qU846JEpW、BV1xQtf6SEHR。
资产：ill `request-journey`；demo `prefill-decode`。
必须覆盖：七步链路（tokenize→排队调度→prefill→decode→logits/采样→detokenize→流式返回）；CPU 活 vs GPU 活；自回归=一次一个字；TTFT 现象观察；同轮多请求的"历史在窗口里"。
算例：8192 token prompt 在 580 tok/s 与 2600 tok/s 引擎上的首字等待差（14s vs 3.2s，说明口径）。
实验：stream on/off 手测首字与总时长（用 1.1 已部署服务）。
产出：手绘泳道图+观察记录。指标回看：TTFT 首次观测、吞吐体感。

## 1.3 为什么第二个字便宜：注意力与 KV 缓存（part1/1-3.md）
核心问题：模型生成时怎么"记住"前文？
上章：链路七步、自回归。下一章依赖：Layer type 字段、KV 随长度增长。
源章：v1/03、v1/04、v1/05。笔记：BV1UMEv68E3C。
资产：demo `attention`、`kv-cache`。
必须覆盖：三种记笔记法（全注意力/滑窗/线性-GDN）；KV 缓存省什么计算；Layer type 字段查证；带历史 vs 不带历史的请求差异。
算例：8B 模型单 token KV 体积（层数×KV头×维度×2×字节代入，如 32×8×128×2×2B=128KB/token）。
实验：同 prompt 带/不带历史两次请求的 TTFT 对比（1.1 服务）。
产出：给外行解释 KV 缓存的一段话。指标回看：TTFT（历史长短的影响）。

## 1.4 聊着聊着就忘了：上下文窗口（part1/1-4.md）
核心问题：为什么长对话会"失忆"？
上章：KV 直觉、Layer type。下一章依赖：上下文长度参数、reasoning budget、模型选型词汇。
源章：v1/08、v1/06。笔记：BV1xQtf6SEHR、BV1nVVr6QEFW→BV1nVVr6QEFq。
资产：ill `kv-growth-curves`。
必须覆盖：上下文窗口与截断；token 估算；reasoning budget；混合架构为何 KV 涨得慢（承 1.3）；失忆复盘。
算例：窗口 32K vs 256K 在 8B 模型上的 KV 体积差（用 1.3 单 token 值×2 的倍数关系推）。
实验：多轮对话直到触发截断，记录失忆表现。
产出：失忆复盘记录。指标回看：显存占用（KV 分量）、TTFT（长上下文 prefill 变慢）。

## 1.5 第一次选型：我的机器该跑哪个模型（part1/1-5.md）
核心问题：货架上那么多模型，我跑哪个？
上章：窗口/失忆、显存增长。下一章依赖：显存三本账动机、"总参vs激活参"词汇。
源章：v1/10、v1/09。笔记：BV14UEY6WEW7、BV1GtbL63Ekt。
资产：ill `model-shelf`、`dense-vs-moe-comic`、`params-total-vs-active`。
必须覆盖：总参vs激活参（稠密/MoE）；30B 甜点位与版本时效；按显存上限反推档位；选型理由写法。
算例：给定 8GB 显存 → 可选档位反推（8B Q4 ✓ / 14B Q4 勉强 / 30B 需量化+页交换）。
实验：跑两个不同规模模型（如 4b vs 8b vs 14b），记录可用性/速度差异。
产出：选型理由一页（T1 完成）。指标回看：显存占用、吞吐（体感对比）。

## 2.1 显存三本账（part2/2-1.md）
核心问题：这个模型+这个上下文，我的卡装得下吗？
上章：选型、显存增长现象。下一章依赖：三本账公式、OOM 判断。
源章：v4/01、v4/02、v1/05。笔记：BV1UMEv68E3C。
资产：demo `vram-budget`；ill `kv-growth-curves`（复用）。
必须覆盖：存储三层级；三本账公式（权重/KV/缓冲）；KV 全公式（层数×KV头×维度×2×长度×字节）；GB/GiB 口径；OOM 与降档方案；naive vs Layer type 修正（埋雷：256K 64G→16G 全程留到 5.4 翻案）。
算例：千问 27B、256K 上下文、AWQ INT4：20G 权重 + 16G KV + 缓冲 → 44G 双卡成立的完整代入。
实验：填三本账表 → nvidia-smi 实测对照，解释误差。
产出：显存计算器（表格/脚本）（T2）。指标回看：显存占用（精确化）。

## 2.2 为什么要量化：三笔账（part2/2-2.md）
核心问题：54G 的模型怎么塞进 24G 的卡？
上章：三本账。下一章依赖：位宽概念、档位语境。
源章：v2/06（为什么量化节）、v4/03（带宽决定 decode 引子）、v2/01（动机段）。笔记：BV1UMEv68E3C。
资产：ill `why-quantize`。
必须覆盖：装不下/搬不动/损失小三笔账；权重÷4→速度×4 的带宽账；量化=交易不是免费；"更快更省更隐私"的成立条件。
算例：1TB/s 带宽喂 54G vs 13.5G 权重的 decode 上限（18 vs 74 tok/s，标注理论上限口径）。
实验：无（用 2.1 数据推演）。
产出：给外行的三句话。指标回看：吞吐（理论上限换算）、显存占用。

## 2.3 位宽的世界：浮点、FP8 与整数格（part2/2-3.md）
核心问题：砍位宽到底砍掉了什么？
上章：量化动机。下一章依赖：档位命名、GGUF 语境。
源章：v2/01、v2/02、v2/03（档位表与 INT 路线）。笔记：BV14yT46kE5T、BV17QN66tEKX。
资产：demo `quant-grid`；ill `fp-bit-layout`、`fp8-nvfp4-blocks`、`quant-size-ladder`。
必须覆盖：位段解剖（符号/指数/尾数）；FP32/FP16/BF16/TF32 取舍；FP8 E4M3/E5M2 分法与 NVFP4 两层缩放；INT 路线与档位表（Q8~IQ1）；位宽-体积阶梯；训练精度vs部署精度。
算例：27B 各档体积阶梯（54G→13.5G），含数量级检查。
实验：Python struct 拆 1.0 字节 + np.float16(65520) 溢出。
产出：位段解剖笔记。指标回看：显存占用（阶梯）。

## 2.4 打开 GGUF：文件、超级块与档位（part2/2-4.md）
核心问题：Q4_K_M 这个名字里藏着什么？
上章：位段、档位。下一章依赖：量化误差与质量评估。
源章：v2/03（GGUF 四段/超级块/_M 混合）、v2/04、v2/05。笔记：BV1UMEv68E3C。
资产：ill `gguf-file-layout`、`super-block-anatomy`；demo `outlier-comic` 命名核对（outlier-comic 为 ill）。插画名 `outlier-comic`。
必须覆盖：GGUF 四段结构与 metadata（file_type=15）；safetensors 对比；超级块 4.5bit 账（144B/256）；_M 混合策略→4.8；W8A16 命名与 weight-only；异常值与 per-channel（Marlin 为什么快，压缩）。
算例：超级块 144×8÷256=4.5 bit 完整推导。
实验：llama.cpp 加载打印逐张量类型，对照四段结构。
产出：自己模型的 GGUF 走查。指标回看：显存占用（名义 vs 实测折扣）。

## 2.5 量化的代价与甜点位（part2/2-5.md）
核心问题：Q4 之后为什么不能再省？
上章：档位与超级块。下一章依赖：量化前后对比方法论。
源章：v2/06 全部。笔记：BV1GtbL63Ekt、BV1i6Y86AEv3、BV1hLtG6ZE5M。
资产：ill `hallucination-vs-quant`、`quant-sweet-spot`。
必须覆盖：位宽-质量曲线与拐点；误差累积与自回归放大；幻觉；KV 量化取舍；甜点位=边际收益分界；"IQ3 起步 Q4 标配"口径与 MoE 例外。
算例：Q8/Q4/IQ3 三档显存+速度+质量的对照表完成过程。
实验：同模型三档量化：显存/速度/同题盲测（T5）。
产出：量化对比报告（T5）。指标回看：显存、吞吐、TTFT 全套前后对照。

## 3.1 会看表：TTFT、TPOT 与吞吐（part3/3-1.md）
核心问题："快"到底是哪种快？
上章：量化对比已用粗测。下一章依赖：并发语义、指标曲线。
源章：v3/01、v1/07（口径）。笔记：BV1nVVr6QEFq、BV1Y2LX61EjQ（波动）。
资产：demo `prefill-decode`（复用）。
必须覆盖：TTFT/TPOT/吞吐正式定义与手测；吞吐vs单路速度；单并发=个人真实指标；PP/TG 口径；最高速度≠稳定复现；置信区间直觉。
算例：同一组 10 次测量的波动整理（均值/极差），判断"该不该信"。
实验：curl 脚本手测三指标 × 10 次，与 llama-bench 互验。
产出：个人指标表 v1。指标回看：三指标正式化。

## 3.2 并发与批处理：GPU 为什么不怕人多（part3/3-2.md）
核心问题：10 个人同时用，为什么不是慢 10 倍？
上章：三指标。下一章依赖：引擎调度、多用户。
源章：v3/04、v3/03（PagedAttention）。笔记：BV1i6Y86AEv3、BV1YxjU6qEuw。
资产：demo `batching`、`paged-attention`。
必须覆盖：batch 复用权重原理；连续批处理 vs 静态；PagedAttention（KV 显存页管理）；并发-单路速度-总吞吐三方关系；并发翻倍的显存后果（KV×N）。
算例：32K 上下文 8B 模型 8 路并发的 KV 总账（128KB/token × 32K × 8 ≈ 32GB → 必须页管理/量化）。
实验：1/2/4/8 路并发压测，填三指标表。
产出：并发行为笔记。指标回看：并发（定义）、吞吐/TPOT（并发维度）。

## 3.3 引擎在忙什么：调度、CUDA Graph 与内核（part3/3-3.md）
核心问题：同样的卡，换引擎为什么差一倍？
上章：并发。下一章依赖：瓶颈定位的"调度"维度。
源章：v3/03（调度）、v3/05、v3/07（压缩）、v3/08（毫秒分解）。笔记：BV14yT46kE5T、BV1qU846JEpW。
资产：demo `cuda-graph`。
必须覆盖：引擎三件事（排队/组批/KV 管理）；CUDA Graph 消发射开销与显存代价；内核（Marlin weight-only/FlashAttention）各自解决什么；CPU 何时成瓶颈（毫秒分解）。
算例：500 内核×5µs=2.5ms vs 10ms 预算的 CUDA Graph 账。
实验：vLLM/llama.cpp 开关 CUDA Graph（或 --no-cudagraph）对照。
产出：毫秒级时延分解表。指标回看：TTFT/TPOT（调度贡献）。

## 3.4 找瓶颈：roofline 与对照实验（part3/3-4.md）
核心问题：我的服务卡在哪一环？
上章：引擎内幕。下一章依赖：优化决策（3.5）、多用户（第四部分）。
源章：v4/03（roofline）、v5/08（口径批判）、v1/10（口径部分）。笔记：BV1YxjU6qEuw、BV1kgN26SECZ。
资产：demo `roofline`。
必须覆盖：roofline 两区与工作点；prefill/decode 分别定位；单变量对照实验设计；口径批判五问（谁测的/什么负载/什么口径/能否复现/谁受益）。
算例：2080Ti 616GB/s ÷ 权重 16.5GB ≈ 37 tok/s 理论上限 vs 实测的差距解释（双卡聚合/量化口径——回链 4.1 预告）。
实验：batch/位宽/上下文三组单变量对照，观察工作点移动。
产出：性能报告=双曲线+瓶颈结论（T3+T4）。指标回看：全六指标的瓶颈定位用法。

## 3.5 投机解码与 MTP：不换卡的提速（part3/3-5.md）
核心问题：不换卡还能怎么快？
上章：瓶颈定位。下一章依赖：多用户场景的取舍。
源章：v3/06、v5/03。笔记：BV1qGh56JEj2。
资产：demo `speculative`；ill `n-value-curves`。
必须覆盖：draft-verify 与接受率；任务确定性（科学>代码>创意）；N 值甜点；加速比 1+N×p 线性上界与严格式；代价（prefill 变慢/显存/PP 不兼容）；MTP/DFlash/DSpark 区别与厂商归属（存疑标注）。
算例：N=4、p=0.7 的加速比代入（线性 3.8×上界；严格式 1+Σp^k≈2.7 的计算过程）。
实验：开/关 MTP 对照，按任务类型（代码/问答）分记。
产出：MTP 开关决策记录。指标回看：TPOT/TTFT（优化前后）。

## 4.1 双卡怎么放：TP vs PP（part4/4-1.md）
核心问题：两张小卡为什么常常赢一张大卡？
上章：全书前三部分。下一章依赖：互联通信账。
源章：v4/06、v5/01。笔记：BV1YxjU6qEuw、BV1dx3Q6iEbZ、BV1nVVr6QEFq。
资产：demo `tp-vs-pp`；ill `pp-pipeline-anim`。
必须覆盖：显存拼接与权重切分；TP（层内切+all-reduce）vs PP（按层接力+流水重叠）；PP 让 prefill 重叠、decode 不重叠；580→800 案例全程；单变量对照纪律。
算例：8K prompt 拆 4×2K 的单卡 8 拍 vs 双卡 5 拍完整甘特推演（+50%）。
实验：单卡 vs 双卡 PP 同 prompt 集基准。
产出：双卡基准报告。指标回看：吞吐（+50%）、TTFT（prefill 重叠后）。

## 4.2 卡间互联：NVLink、PCIe 与通信账（part4/4-2.md）
核心问题：卡和卡之间的路有多宽？
上章：TP/PP。下一章依赖：大模型路线、选型。
源章：v4/07、v4/06（通信公式）。笔记：BV1Y2LX61EjQ、BV12ZEN6CEho。
资产：demo `pcie-bandwidth`；ill `interconnect-map`。
必须覆盖：PCIe lane/代际/南桥；NVLink 桥接带宽；payload=hidden×token×字节；129 次 all-reduce；带宽敏感 vs 延迟敏感；topology 查看；矿卡飞线案例。
算例：4096 token×5120 hidden×2B=42MB，×129 次≈5.4GB/前向 的完整通信账。
实验：读 nvidia-smi topo -m 解释自己机器拓扑（或案例数据复算）。
产出：互联带宽笔记。指标回看：吞吐（通信惩罚）、TTFT。

## 4.3 大模型的另两条路：统一内存与 offload（part4/4-3.md）
核心问题：176B 怎么可能在几张老卡上跑？
上章：互联。下一章依赖：多用户压测的负载语境。
源章：v4/08、v5/05、v5/06。笔记：BV1otY26LE8w、BV1xQtf6SEHR、BV14UEY6WEW7。
资产：demo `unified-memory`；ill `qwen-arch-ngram`。
必须覆盖：MoE 完整架构（路由/专家/n-gram 外挂——承接 1.5）；统一内存零拷贝 vs 划线（苹果系区分）；offload 分层（显存/内存/U盘）与冷热数据；176B=125B+51B 拆账；395 vs 495 平台边际价格。
算例：176B Q4≈72G 的拆账（主模型 21G 常驻+51G 表按需读）与带宽上限核对。
实验：（可选）CPU offload 对比：层放内存 vs SSD 的速度差。
产出：大模型路线决策树。指标回看：吞吐（offload 下的崩塌与可用）。

## 4.4 多用户上线：从玩具到服务（part4/4-4.md）
核心问题：怎么变成同事也能用的服务？
上章：全部。下一章依赖：TCO 的负载数据。
源章：v3/04（深化）、v4/09（供电/散热）。笔记：BV1KKE96CELt。
资产：无新资产；复用 batching/pcie 观点。
必须覆盖：局域网暴露与并发上限（--max-num-seqs 类参数概念）；KV 预算与并发数关系；供电/散热底线（600W→1100W 冗余）；压测方法（2/4/8 用户脚本）；指标退化记录纪律。
算例：4 用户 × 8K 上下文 的 KV 预算 vs 44G 的分配方案。
实验：2/4/8 用户脚本压测，记录三指标退化曲线。
产出：多用户压测报告（T6）。指标回看：并发/TTFT/TPOT（退化曲线）。

## 5.1 三要素读穿一张卡（part5/5-1.md）
核心问题：规格表上哪个数字决定我的体验？
上章：全书实战。下一章依赖：市场判断。
源章：v4/03（剩余）、v4/04、v4/05。笔记：BV1E5426eE5X、BV17QN66tEKX、BV14yT46kE5T。
资产：ill `arch-generations`、`hbm-vs-gddr`。
必须覆盖：容量/带宽/算力三要素归类法；架构代际与位宽格式支持（FP8 起点 Ada 消费卡、NVFP4 Blackwell）；HBM vs GDDR 结构与带宽来源；ECC 代价；规格表逐字段提取法。
算例：带宽=位宽×等效频率÷8 完整推导（2080Ti 352bit×14Gbps÷8≈616GB/s）。
实验：两张陌生卡规格表做三要素速判（T8 素材）。
产出：规格表解读模板（T8）。指标回看：显存/吞吐（选型映射）。

## 5.2 显卡市场生存指南（part5/5-2.md）
核心问题：同一笔钱怎么花出双倍显存？
上章：三要素。下一章依赖：TCO 的购置成本。
源章：v4/10、v5/04。笔记：BV1hLtG6ZE5M、BV1i6Y86AEv3、BV17QN66tEKX。
资产：ill `gpu-map`。
必须覆盖：矿卡/魔改卡/SXM2 风险清单；价格×显存地图（神卡区/温饱线/智商税区）；消息面与价格；能效比；"以终为始"选卡顺序；机会成本。
算例：3080 20G（3000 元档）vs 两张 2080Ti 22G（3500 元）的显存/带宽/风险三栏对比。
实验：给 3 张在售二手卡按地图打分（网页实操）。
产出：自己的天梯表。指标回看：显存/吞吐（价格维度）。

## 5.3 成本与决策：本地 vs API 的 TCO（part5/5-3.md）
核心问题：这套机器到底值不值？
上章：压测与选卡。下一章依赖：方法论收官。
源章：v5/07、v4/09（电费）。笔记：BV1KKE96CELt。
资产：ill `utilization-collapse`。
必须覆盖：24h 满载产出账；利用率 η 曲线；API 每百万 token 对比；矿卡折价与精力成本；敏感性分析（利用率±）；买车 vs 打车的边界。
算例：20 元/天 ÷ 864 万 token = 2.3 元/百万（η=100%）与 η=20%→11.6 元、η=5%→46 元 的完整表。
实验：用自己 T6 负载数据代入 TCO 表。
产出：选型与成本决策书（T7）。指标回看：成本（终极口径：每百万 token）。

## 5.4 方法论内化：像 UP 主一样"知道"（part5/5-4.md）
核心问题：下次遇到陌生说法，我怎么自己判断？
上章：全部。收官章。
源章：v6/01、v5/02、v5/08。笔记：BV1UMEv68E3C、BV1kgN26SECZ。
资产：ill `debug-loop`、`layer-type-bars`。
必须覆盖：七模式闭环（先算账/信一手字段/控制变量/口径批判/成本工程/以终为始/开源复现）；KV 100G 翻案完整走查（naive 64G→Layer type→16G）；调用量宣称的口径批判五问；把方法用于新问题的示范。
算例：翻案全程复算（naive 64×8×128×2×256K×2B=64GiB → 1/4 全注意力 → 16GiB）。
实验：选一个网上的性能宣称，完整走四步（查/算/测/复现）。
产出：方法论走查报告（全书收官件，装订所有交付物）。指标回看：全部指标的"可信测量"总结。
