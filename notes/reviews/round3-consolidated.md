# 第 3 轮评审 · 合并修复清单

本轮通用决定（必须遵守）：
- **170HX 算力口径（v2/01 重写）**：拆成三句点名主语——"FP32 那行：0.3 TFLOPS，解锁后 0.3×31≈9.3 TFLOPS。100 TOPS 是另一张表（TF32 张量口径）上的数，与 0.3 和 31 都无关。三张表各列各的，不能互相乘。"
- **CUDA 13 与图灵（v3/09 为准，改 v4/04）**：图灵（SM75）是 CUDA 13 仍支持的最低架构；真正停在 CUDA 12 的是 Volta（V100）/Pascal。v4/04 删"2080Ti 吃不到 CUDA 13"，改为"BF16/FP8/FP4 的硬件加速单元都没有"。
- **90HX PCIe 方向（源笔记为准）**：90HX 出厂锁 PCIe 1.0 x4，解锁（飞线/漏洞）后才到 x16 档（≈PCIe 3.0 x8）。v4/10 改；v4/06/v4/07 写的是 170HX"飞线后仍只有 1.0 x4"，保持；v6/02"刷 BIOS/飞线"词条核对一致。v4/10"怎么验证"里自相矛盾的两句改成一个说法。
- **90HX 显存 = GDDR6X**：v4/05、v5/04 的"GDDR6"改回"GDDR6X（10G 320bit，约 760GB/s）"。
- **裸 decode 口径全书统一**："不开 MTP 约 50 tok/s（带宽理论上限约 60）"。v1/08 首现处写全这句并给"裸 decode"中文注（裸=不开任何加速）；v3/01、v3/03、v3/08、v4/07、v4/09、v5/01 各处统一为"约 50"或引 v1/08。
- **V100 FP16 统一**："口播 130T；官方 112T（PCIe）/125T（SXM2）"。v4/04 补注（v3/07、v5/04 已对）。
- **GDN 译名统一为"门控 Delta 网络"**（expert 判断优先）：v1/06、v5/02 正文改，与 v6/02 词条一致。
- **重复案例定点化**：双卡 PP 实验（8K 拆 4×2K、580→800、600→1000）定点 v3/01，他章一句话+链接；64G→16G 翻案定点 v1/06（v1/01 案例只留"这里有一笔显存疑账，v1/06 翻案"，不给公式和 16G；v1/03/v1/04 案例收到各自主题一句+链接）；"图灵跑 FP8 靠反量化"定点 v2/04（v2/02、v2/05 改链接，v2/04 删本章重复的第二个案例块）；"24G 硬跑 256K 压 Q4"留 v1/04 与 v2/06 两处，其余改链接；v4/07 拓扑降级实验保留、数字账（5G/129次/80G/161MB/13万次）留 v4/06 一处；P106 括注定点 v1/09，v2/06、v5/05 改"（配置见 v1/09）"。
- **编辑注记清除**：v6/03 删"第 2 轮核对时补入……"，引言改"22 期里 21 期进入正文（另 1 期仅作背景提及）"；"出现在 8 个章"改"出现在多个章"；v1/10 表格"待核"改"版本名以模型卡为准"；"代际待核"改"规格待核"。
- **术语表路标**：v1/01 开头加一行"遇到看不懂的词，先查[术语速查表](#/v6/02)；「怎么验证」需要本地模型环境（v1/01、v1/02 已说明），没有环境可跳过，不影响后续阅读"。

## v1
- v1/01 高：首句后加"推理"定义："推理=拿训练好的模型来生成回答的过程（你在聊天窗口等回复，模型干的就是推理），与'训练'相对"
- v1/01：加术语表路标（通用决定）
- v1/01 中：链路行"softmax（把打分变成概率，下一章讲）→ 抽签（行话叫采样）→ detokenize"；"GPU 填充"改"GPU 吞输入"
- v1/01 低："显存"首现加"（显卡上装模型的那块内存，v4/01 详解）"；"满血"注从 v1/05 前移到此处首现
- v1/01 高（案例泄底）：案例块删公式与 16G 结论，只留"这里藏着一笔显存疑账——密码是模型卡里一个叫 Layer type 的字段，[v1/06](#/v1/06) 揭晓"
- v1/02 中：softmax-temp 动画补"▶ 动画：拖温度，看概率分布变尖或摊平"
- v1/03 低："带弯折的变换"补"（弯折=不是一条直线算到底，会拐弯，行话叫非线性）"
- v1/03 中：案例收到"层数是第一个乘数"一句+链接 v1/06
- v1/04 中：他章两笔账压成一句结论+链接（64G/16G → v1/06；24G 压 Q4 → v2/06）
- v1/04 低："函数签名"改"函数定义行"
- v1/05 低："256 KiB"后加"（约 26 万字节）"；千问 27B 口径短注补上（专家条目）
- v1/06 低：三层嵌套括号拆句；"卷积状态"改"另一块固定大小的小仓库（名字叫卷积状态，不用记）"；验证块补"1024 个 1 MiB = 1 GiB"换算行；GDN 译名改"门控 Delta 网络"；800MB 括注精简为"（视频口径，公开规格约 400MB）"
- v1/06 低（v5/02 同步）：v5/02 表格 800MB 后补"（视频口径）"；递归/卷积状态补"（都是固定大小的便签，类比见 v1/06）"
- v1/07 低：修复悬空句"（两个缩写的定义见下一小节），取决于……"重排；PP 双义长括号改正文短句"注意：这是跑分表口径的 PP，与 v4/06 流水线并行的 PP 是两回事"；"40 层"首现补"（视频举例口径，见 v1/03）"；Agent 注统一"（智能体，自动多步调用模型的程序，见 v5/08）"
- v1/08 高：裸 decode 口径统一（通用决定），首现加"裸=不开任何加速"
- v1/08 低："多轮对话历史不清、KV 不降"改"多轮对话的历史不清空，KV 就不会下降"
- v1/09 中：60 字三层括号拆成正文两三句（cs 报告有现成改法）；"IQ 级深压"改"压得极狠的 IQ 档（档位见 v2/03）"
- v1/10 低："待核"改"版本名以模型卡为准"；"代际待核"改"规格待核"；"每步只激活十几 B"改"每步只激活 10B（A10B）"

## v2
- v2/01 高：170HX 段按通用决定重写三句；"图灵"补"（Turing，英伟达 20 系那一代）"；"GA100"补"（A100 用的那颗芯片代号）"
- v2/01 低：表格单元格括注移到正文 TF32 段；五处纯文本"见 v4/04"改 `[架构代际](#/v4/04)` 形式
- v2/02 中：M5 备注拆两句"M5 起原生的是低精度矩阵运算（FP16/INT8）；FP8 苹果全系仍未支持"；"全张量缩放"改"全张量级缩放（整组数共用一个 scale，见 v2/05）"；段末四连"见 X"合成一句；图灵翻案改链接 v2/04
- v2/03 中：AWQ 定义句拆两句（先定义激活值再讲保护思路）；"端侧"→"个人设备/手机与电脑上"、"GUI"→"图形界面"（含卷三标题处若在 v2）；GGUF 括注改"GGUF 是权重容器格式，属于 llama.cpp 那一派生态"
- v2/04 高：删"反量化"小节的重复案例块（BV14yT46kE5T 引语只留一处）；"算力受限/访存受限"改"一头吃算力、一头吃带宽（见 v4/03）"；"jump"补"（jump=成绩猛地掉一档）"
- v2/05 中：图灵全套重复改链接 v2/04，本章只留 Marlin 为什么快
- v2/06 低："24G 硬跑 256K"保留为本卷定点；P106 括注改"（配置见 v1/09）"

## v3
- v3/01 中：旗舰 100 tok/s 补 MTP 口径（通用决定）；本卷成为 PP 实验定点章（确认数字齐全）
- v3/01 低："缓存冷热"改"缓存是冷是热（数据在不在快的地方）"
- v3/03 中：TP worker 与 v3/08 重复 → 半句+链接 v3/08；"两到四成/打三折"统一"只剩两到四成"
- v3/04：PP 实验重复改一句话+链接 v3/01（通用决定）；"40 层"简写可保留
- v3/05 低："star"注"（GitHub 上的收藏数）"前移到这里（v5/07 可留可删）
- v3/06 中：公式密度——正文只留直觉一句"平均猜对几个、每步就多产几个"，线性上界与严格式两个公式挪"怎么验证"；"+1"点破"在第一个被拒的位置之后，主模型还免费多出一个 token"；"接受率差一半收益差近一倍"删或补具体 p 值；嵌套括号"（写脚本、发起工具调用）"
- v3/07 低：FlashQLA 改"口播似为 FlashQLA，实指 FlashInfer 一系"；"逐元素"加"（每个数各自算一遍）"；"四元素点积"改"4 个数相乘再相加，一条指令做完"
- v3/08 中：100+ tok/s 补"（含 MTP）"；双层括号拆句
- v3/09 低：CUDA 13 与图灵表述保持（以本卷为准）；"GFX 设计方案"改"核心设计方案"；项目名"vLLM 2080Ti definitive"统一为"vLLM-2080Ti-Definitive"（全书一致）

## v4
- v4/03 低：动画说明句改"拖并发/权重位宽，看算术强度变化、工作点在屋顶线上滑动"
- v4/04 高：删"2080Ti 吃不到 CUDA 13"（通用决定）；V100 FP16 补口径注；代际表 DP4A 注"SM61（P4/P102/1080Ti）所有，SM60 的 P100 没有"
- v4/05 中："31 倍"口径与 v2/01 统一（31 倍=FP32，100 TOPS=TF32）；GDDR6→GDDR6X（通用决定）；"快小一半"拆句改明确倍数
- v4/06 低："是一词两义，两码事"改"是另一个 PP"；正文加"（多出的 1 次来自输出层）"；"飞线"首现加"（手工焊线补电路，见 v4/10）"；MI250 首现加"（AMD 下一代数据中心卡）"；P2P 首现统一"点对点"；"两条腿"比喻改"prefill 变大的是单次传输量，decode 变多的是传输次数"；"同一 root"加"（root=这些通道的源头，下章讲）"
- v4/07 中：profiler 数字账删（留 v4/06），保留拓扑降级实验与结论；"拿五星"加"（UP 主天梯五星，见 v5/04）"；裸 decode 口径核对（通用决定）
- v4/08 低：900+/200~300 口径注定点 v5/06（此处只留链接）；hello-flash 加"（一个针对该平台的开源优化项目）"；"异构架构"改"CPU、显卡各管各的普通电脑"
- v4/09 低：864 万 token/天 补"（含 MTP 的峰值口径）"；"1L 迷你主机"加"（机身体积约一升）"；turbostat 加定位"（内核自带的读 CPU 实际功耗工具）"
- v4/10 高：90HX PCIe 方向改正（通用决定）；"怎么验证"两句矛盾改一个说法；"消息面"改"一条消息就能让价格一夜起飞"；"刀法"首现加"（厂商切分档位的手法）"（若 v4/05 已有则此处链接）

## v5
- v5/01 中：重复嵌入的两个动画删一个或注明"与 v1/05、v4/02 同款交互"；MTP 全称重复收敛（第二次起只中文名+链接）；AWQ/TCO 全称同理；"缓存命中"解释改"或实测含其他开销，需逐项排查"；裸 decode 口径统一"约 50"；v5/01 补"（此后迭代到 v0.1.12，见 v3/05）"接版本时序
- v5/02 低：双免责声明合一；GDN 译名改"门控 Delta 网络"；800MB 补"（视频口径）"；章题 100G 加"（章题的 100G 是视频标题口径，正文算出来是 64G/120G）"
- v5/03 中：颜色指代删（DSpark>MTP>dFlash 直接排序）；加速比标注"线性上界口径（严格式 1+p+…+p^N，见 v3/06）"
- v5/04 低：温饱线加"（预算只求够用、不追求快）"；3080 20G 五星与"3000 起不推荐"边界说明；90HX GDDR6X（通用决定）；"30B Q4 约 17G"加"（视频口播值；27B 同档见 v4/02 对照表）"；ECC"降 20%"加"（视频实测口径，见 v4/05）"；V100 表已对（确认）
- v5/05 低："缓存未热"改"刚开跑时数据还没搬进快的地方，所以偏慢"；P106 括注改"（配置见 v1/09）"；dd/CrystalDiskMark 加定位半句；表格"100G 放不下 72G"加"（同 v1/09：盘上还要放系统、缓存与 KV）"
- v5/06 低："节点"改"每台机器"；"类稠密模型"加"（每步要动全部参数的模型，与 MoE 相对）"；口径注移段末；mbw/sysbench 加定位；900+/200~300 口径注为本卷定点

## v6
- v6/01 低：profiler 加"（给程序逐项记时间账的工具）"；TCO 全称重复收敛（第二次中文名+链接）；"可能只有 A10B"核对口径
- v6/02 中：死条目处理——SOTA 删或在 v1/10 补首现（建议 v1/10"甜点位"处补"SOTA（state of the art，当前最强）一次"）；"电老虎""权威性错觉""显存-算力匹配""显存容量档位""端到端时间"删或改正文用词；"端到端"链接改实际章
- v6/02 低：FLOPS/TOPS 词条改"每秒能做多少次运算；FLOPS 指浮点，TOPS 各厂口径不一（见 v2/01）"；"V100｜伏特代"改"Volta 代"；GDN 词条确认"门控 Delta 网络"（正文 v1/06、v5/02 本轮已改齐）
- v6/03 低：删编辑过程注；"22 期里 21 期进入正文（另 1 期仅作背景提及）"；"出现在 8 个章"改"出现在多个章，表列为主要出处"；「」引号统一为全书通用引号
